# نظام التحكم في الوصول القائم على الأدوار (RBAC) الديناميكي

تم تحديث النظام بالكامل ليعتمد على هيكلية أمنية متطورة وديناميكية. هذا المستند يشرح كيفية عمل النظام الجديد وكيفية إدارته.

---

## 1. التغيير الجذري: من "الأدوار" إلى "الصلاحيات"
في النظام القديم، كان الكود يتحقق من نوع المستخدم (مثلاً: هل هو مدير؟ هل هو مدرس؟). أما الآن، فالكود لا يهتم بمن هو المستخدم، بل يهتم فقط بـ **"ما هي الصلاحيات التي يمتلكها؟"**.

## 2. هيكلية الصلاحيات (Resource:Action)
تتبع الصلاحيات الآن نمطاً موحداً يجمع بين المورد والإجراء:
*   `users:read`: قراءة بيانات المستخدمين.
*   `courses:create`: إضافة دورات جديدة.
*   `sessions:join`: الانضمام إلى جلسة تعليمية.

## 3. الوسائط الأمنية (Middlewares) الجديدة
تم استبدال نظام التحقق القديم بوسيطين ذكيين:

### أ. `authorizeResource("resource_name")`
هذا الوسيط يقوم تلقائياً بتحديد الصلاحية المطلوبة بناءً على نوع طلب الـ HTTP:
*   طلبات `GET` تتطلب صلاحية `read`.
*   طلبات `POST` تتطلب صلاحية `create`.
*   طلبات `PATCH/PUT` تتطلب صلاحية `update`.
*   طلبات `DELETE` تتطلب صلاحية `delete`.

**مثال:**
`router.get("/", authenticate, authorizeResource("courses"), controller.list);`
سيبحث هذا السطر تلقائياً عن صلاحية `courses:read` للمستخدم الحالي.

### ب. `authorize("permission_code")`
يستخدم للتحقق من صلاحية محددة لا تتبع النمط القياسي لـ CRUD.

## 4. تهيئة النظام (Seeding)
تم دمج جميع عمليات إنشاء الصلاحيات والأدوار في ملف بذر واحد: `prisma/seeders/permissionsSeeder.js`. 
عند تشغيل هذا الملف:
1.  يتم إنشاء كافة الصلاحيات المعرفة في النظام.
2.  يتم إنشاء الأدوار الافتراضية (admin, teacher, student, etc).
3.  يتم منح **كافة الصلاحيات** تلقائياً للأدوار الإدارية (admin, super_admin).

## 5. الثوابت المركزية (Constants)
جميع أكواد الصلاحيات معرفة الآن في مكان واحد: `src/Constants/permissions.constants.js`. يجب دائماً إضافة أي صلاحية جديدة هناك أولاً.

## 6. تحسينات الأداء
يتم جلب صلاحيات المستخدم عند تسجيل الدخول وتخزينها كـ `Set` في الطلب (Request Object)، مما يجعل عملية التحقق سريعة جداً ولا تستهلك موارد قاعدة البيانات في كل طلب.

## 7. ملفات تم حذفها (تطهير النظام القديم)
تم حذف جميع ملفات `authorization.js` التي كانت موجودة داخل المجلدات الفرعية للموديولات، حيث لم تعد هناك حاجة لتعريف الصلاحيات بشكل ثابت (Static) لكل موديول على حدة.

## 8. نصائح للمطورين
*   لا تستخدم أبداً `if (user.role === 'admin')` في الكود. استخدم `req.user.hasPermission('code')` أو الوسائط الأمنية.
*   عند إضافة ميزة جديدة، أضف صلاحياتها في ملف الثوابت وشغل الـ Seeder.
*   تذكر أن الـ `super_admin` يتجاوز جميع الفحوصات الأمنية تلقائياً.

---
تم تصميم هذا النظام ليكون مرناً وقابلاً للتوسع دون الحاجة للمساس بالكود البرمجي الأساسي عند تغيير قواعد العمل.
